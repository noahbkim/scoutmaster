import json

from django import http
from django.shortcuts import render
from django.template import loader

import scraper
from constants import *

# Create your views here.
def index(request):
    return render(request, "info/index.html", {})

def opr(request, event):
    event_data = scraper.scrape("event/" + event)
    event_matches = scraper.scrape("event/" + event + "/matches")
    context = {"event": event, "website": data["website"]}
    return render(request, "info/opr.html", context)
